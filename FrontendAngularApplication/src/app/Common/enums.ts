export enum SnackbarType {
  Success = 'Success',
  Error = 'Error',
}

export enum Cookies {
  AuthToken = 'AuthToken',
}

export enum Role {
  SuperModerator = 'SuperModerator',
  Moderator = 'Moderator',
  Student = 'Student',
}
